{
  "name": "wwofdrslide",
  "version": "1.0.0",
  "description": "Step into the world of Dr. Slide!",
  "main": "server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd server && node server.js",
    "install": "cd client_wwofdrslide && npm install && cd ../server && npm install",
    "dev": "concurrently \"cd server && npm run watch\" \"cd client_wwofdrslide && npm start\"",
    "build": "cd client_wwofdrslide && npm run build",
    "seed": "cd server && npm run seed"
  },
  "author": "Emmanuel Lakis",
  "license": "ISC",
  "dependencies": {
    "apollo-server-caching": "^3.3.0",
    "concurrently": "^8.2.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "mongodb": "^6.0.0",
    "watch": "^1.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
